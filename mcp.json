{
  "mcpServers": {
    "silica-core": {
      "command": "uv",
      "args": [
        "run",
        "--project",
        "${CLAUDE_PLUGIN_ROOT}",
        "--extra",
        "mcp",
        "--extra",
        "dense",
        "silica",
        "mcp",
        "--retrieval",
        "local-hybrid"
      ]
    }
  }
}
